import React from "react"
import BackToTop from "../components/BackToTop"

export default ({ data }) => (
    <div style={{ width: "auto", height: "1500px", color: "tomato", padding: "20px" }}>
        <BackToTop />
        <h1 style={{textAlign: "center"}}>Scroll down </h1>
    </div>
)
